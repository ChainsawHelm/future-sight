import { HealthScoreView } from '@/components/views/health-view';
export const metadata = { title: 'Health Score — Future Sight' };
export default function HealthPage() { return <HealthScoreView />; }
