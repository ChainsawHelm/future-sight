import { ExpensesView } from '@/components/views/expenses-view';
export const metadata = { title: 'Expense Reports — Future Sight' };
export default function ExpensesPage() { return <ExpensesView />; }
