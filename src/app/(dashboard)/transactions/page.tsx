import { TransactionsView } from '@/components/views/transactions-view';

export const metadata = {
  title: 'Transactions — Future Sight',
};

export default function TransactionsPage() {
  return <TransactionsView />;
}
