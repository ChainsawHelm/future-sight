import { DashboardView } from '@/components/views/dashboard-view';

export const metadata = {
  title: 'Dashboard — Future Sight',
};

export default function DashboardPage() {
  return <DashboardView />;
}
