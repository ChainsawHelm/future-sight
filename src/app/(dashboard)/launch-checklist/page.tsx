import { LaunchChecklistView } from '@/components/views/launch-checklist-view';
export const metadata = { title: 'Launch Checklist — Future Sight' };
export default function LaunchChecklistPage() { return <LaunchChecklistView />; }
